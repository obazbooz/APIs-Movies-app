// 'use strict';

// export const clearDOMElement = (DOMElement) => {
//   DOMElement.innerHTML = '';
// };

// export const createDOMElementWithID = (tag, options) => {
//   const { id } = options || {};

//   const element = document.createElement(tag);

//   if (id != null) {
//     element.id = id;
//   }

//   return element;
// };

// export const createDOMElementWithClass = (tag, options) => {
//   const { class } = options || {};

//   const element = document.createElement(tag);

//   if (class != null) {
//     element.class = class;
//   }

//   return element;
// };

// export const getDOMElement = (id) => {
//   return document.getElementById(id);
// };
